<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="FlipKit.Desktop.Views.AiScanConsentDialog"
        Title="Before your first AI scan"
        Width="520" Height="380"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        ShowInTaskbar="False">

    <Border Padding="24" Background="{DynamicResource SystemRegionBrush}">
        <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,*,Auto" RowSpacing="12">

            <TextBlock Grid.Row="0" Text="🔒 Before your first AI scan"
                       FontSize="18" FontWeight="Bold"/>

            <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="13" Opacity="0.85">
                FlipKit sends card images to third-party AI services to identify your cards.
                Your images leave your machine each time you scan.
            </TextBlock>

            <Border Grid.Row="2" BorderBrush="{DynamicResource SystemBaseLowColor}"
                    BorderThickness="1" CornerRadius="6" Padding="14"
                    Background="{DynamicResource SystemChromeLowColor}">
                <StackPanel Spacing="8">
                    <TextBlock FontSize="13" FontWeight="SemiBold">What gets sent</TextBlock>
                    <TextBlock TextWrapping="Wrap" FontSize="12" Opacity="0.85">
                        • <Run FontWeight="SemiBold">CardSight</Run> (api.cardsight.ai) — card image(s) only, used for the first-pass card recognition.
                    </TextBlock>
                    <TextBlock TextWrapping="Wrap" FontSize="12" Opacity="0.85">
                        • <Run FontWeight="SemiBold">OpenRouter</Run> (openrouter.ai) — card image(s) as JPEG + a text prompt, used as the fallback.
                        OpenRouter routes each request to the AI model you select (Google, Anthropic, OpenAI, etc.).
                    </TextBlock>
                    <TextBlock TextWrapping="Wrap" FontSize="12" Opacity="0.85">
                        Nothing else is sent. No account data, inventory data, or pricing data leaves FlipKit.
                    </TextBlock>
                </StackPanel>
            </Border>

            <TextBlock Grid.Row="3" TextWrapping="Wrap" FontSize="11" Opacity="0.65">
                Each provider stores and processes images under their own privacy policy.
                Review them at cardsight.ai and openrouter.ai before continuing.
            </TextBlock>

            <StackPanel Grid.Row="6" Orientation="Horizontal" Spacing="8"
                        HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button x:Name="DenyButton" Content="Deny" Padding="20,8"/>
                <Button x:Name="AcceptButton" Content="Accept"
                        Classes="accent" Padding="20,8"/>
            </StackPanel>

        </Grid>
    </Border>
</Window>
